# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# 虚拟环境
venv/
.venv/
env/

# 环境变量与密钥
.env

# 数据库
*.db
*.sqlite
*.sqlite3

# 测试 / 缓存
.pytest_cache/
.cache/
.mypy_cache/
.pytest_tmp/
_tmp_pytest/

# 评测报告（每次运行生成，带时间戳）
eval/report_*.json
eval/ablation_*.json
# 注意：eval/reproduce_report.json 是**要提交**的 —— 它是 README 里
# 那些数字（测试数 / 用例数 / 编造链接 0）的唯一可对照物，含环境指纹。

# IDE
.idea/
.vscode/

# 本地推送辅助脚本（不上传）
push-to-github.bat

# 前端
frontend/node_modules/
frontend/dist/
